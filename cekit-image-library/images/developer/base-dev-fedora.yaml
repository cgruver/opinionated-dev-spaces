name: dev-tools/base-fedora
from: quay.io/fedora/fedora:42
version: latest
packages:
  manager: dnf
  manager_flags: "--disableplugin=subscription-manager --setopt=tsflags=nodocs --setopt=install_weak_deps=0"
modules:
  repositories:
  - path: "../../modules"
  install:
  - name: dev-base